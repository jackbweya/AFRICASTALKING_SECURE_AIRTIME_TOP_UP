# To Do:

- Write a function secure_topup(number, amount) that:
  1. Calls the SIM Swap API via Insights.
  2. Blocks airtime top-ups if a SIM swap occurred in the last 7 days.
  3. Logs safe vs blocked transactions for auditing.


function secure_topup(number, amount){
    // Sim Swap 
}